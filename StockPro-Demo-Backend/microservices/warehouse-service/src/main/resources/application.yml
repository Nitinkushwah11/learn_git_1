server:
  port: 8083 # Standard port for Warehouse Service

spring:
  application:
    name: warehouse-service

  datasource:
    url: jdbc:mysql://localhost:3306/warehouse_db
    username: root
    password: Nitin@123 

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        acknowledge-mode: auto
        prefetch: 1

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/